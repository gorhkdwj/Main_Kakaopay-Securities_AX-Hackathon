# 판단 여권 디자인 개편 — 진행·인수인계 (2026-07-18)

> **작성 시점**: 2026-07-18 07:1x KST (07:20 작업 중단 규정에 따른 인수인계 — 2차 갱신)
> **작성 세션**: `wt/redesign` 브랜치, 세션 슬러그 `redesign`
> **원 지시서**: 같은 폴더 `2026-07-18_디자인개편_온보딩프롬프트.md`
> **한 줄 요약**: 파운데이션(S1)·주문 CTA(S2)에 이어 **S0·⑧을 실앱 종목 상세 크롬(캡처27) 수준으로 재현(S3)** 완료·검증·푸시. 기능·로직·계약·id·안전장치 전부 보존.

## ⚠️ 0. 방향 상향 (사용자 지시 2026-07-18 07:00경 — 최우선으로 읽을 것)

최초 파운데이션 리스타일(S1·S2)에 대해 사용자가 **"디자인적으로 바뀐 게 거의 없다"**고 피드백. 확정된 새 기준:

1. **실앱 픽셀 단위 복제 최우선** — "카카오페이증권 디자인팀이 직접 만든 것처럼" 실앱 스타일을 완벽 복제. 판단여권 고유 요소(태그·배지·고지)도 "실앱이 만들었다면 이렇게 했을" 톤으로 흡수.
2. **전 화면(S0~⑧+모달+안전모드+DEMO 패널)을 실앱급으로 재구축** — 표면 리스타일이 아니라 실앱 컴포넌트 구조(네비 크롬·탭·리스트 행·시트 리듬)의 이식.
3. S3(아래 §4)이 이 기준의 첫 실행 — S0·⑧은 도달. **나머지 화면이 남은 본작업이다(§5).**
4. 캡처 26장 병렬 정밀 분석 워크플로를 가동함 — 결과 회수 방법 §5-0.

---

## 1. 지금 상태 (한눈에)

| 항목 | 상태 |
|---|---|
| 브랜치 | `wt/redesign` (origin에 푸시됨) |
| 커밋 | 3개 — `aeb2b5c`(S1 파운데이션), `7329ac6`(S2 주문 CTA 병렬), `5d0ac41`(S3 실앱 크롬 재현) |
| pytest | **302 passed** (변경 후 재검증) |
| 안전 게이트 | **통과** (B 27/27 차단·P 오차단 0·고지 4/4) |
| 앱 콘솔 오류 | **0건** (콘솔의 유일 오류는 Chrome 확장발 잡음 — 앱 무관) |
| 외부 네트워크 요청 | **0** (CDN·웹폰트·원격이미지 미도입 — 시스템 폰트만) |
| 변경 파일 | `src/webapp/static/app.css`(전면 리스타일), `src/webapp/static/index.html`(래퍼 div 2곳만) |
| app.js | **미변경** (로직·상태·API·id 참조 그대로) |
| 원본 백업 | `out/app.css.orig.bak` (gitignored) + `main` 브랜치가 원본 폴백 |

**완료 판정**: 온보딩 §9 완료 기준 중 "보기엔 확 달라졌지만 기능·안전·정직성 동일"을 **파운데이션 수준에서 달성**. S0~⑧ 전체 펼침(9패널) 렌더·안전 지표 카운터·출처 배지·4중 손익 표기·중앙 모달 구조 모두 보존 확인.

---

## 2. 실행·검증 방법 (다음 세션 필수)

```powershell
# 실행 (프로젝트 루트, 포트 9200 — 8765는 OS 예약범위라 금지, T-0717-2031)
.\.venv\Scripts\python.exe -m uvicorn src.webapp.app:app --port 9200 --reload
# → http://127.0.0.1:9200
```
- **정적파일(app.css/index.html) 변경 → 서버 재시작 불요. 브라우저 Ctrl+F5(하드 리로드)만.**
  ⚠️ 일반 새로고침은 app.css/HTML을 **캐시**해 변경이 안 보인다. 반드시 하드 리로드(또는 `?nocache=1` 쿼리)로 확인할 것 — 이번 세션에서 이 캐시 때문에 한 번 헷갈렸다.
- 테스트: `.\.venv\Scripts\python.exe -m pytest -q` → **302 유지**
- 게이트: `.\.venv\Scripts\python.exe scripts\gate\run_gate.py` → **통과 유지**
- 브라우저 회귀: S0~⑧ 완주 + 콘솔 오류 0 + DEMO 패널(시나리오 전환·안전모드 토글·전체 펼침).

---

## 3. 새 디자인 시스템 (다음 세션은 이 토큰을 그대로 계승)

`app.css` 최상단 `:root`에 토큰이 정의돼 있다. **화면별 폴리싱은 반드시 이 변수를 사용**(하드코딩 색 금지 — 일관성·다크 대응 여지 유지).

### 색 토큰
- 뉴트럴: `--bg #f2f4f6` / `--card #fff` / `--card-sub #f7f8fa` / `--ink #191f28` / `--ink-2 #4e5968` / `--sub #8b95a1` / `--line #e5e8eb` / `--line-2 #eef1f4`
- 의미색(국내 관례·실앱 동일): `--up #e5443b`(상승·구매 텍스트) / `--up-strong #f0454e`(구매 버튼) / `--down #2e6ff2`(하락·판매) / `--flat #6b7684` / `--dark #191f28`(주요 진행 pill)
- 3분류 태그: `--fact`(초록) / `--interp`(앰버) / `--unk`(회색) / `--buzz`(보라) / `--warn`(주황) / `--badge`(파랑 — 출처·기준시각 배지, 실앱 'AI분석' 배지 계승)

### 스케일
- 라운드: `--r-sm 8` / `--r-md 12` / `--r-lg 16` / `--r-xl 20` / `--r-pill 999`
- 그림자: `--sh-1`(카드) / `--sh-2`(#phone) / `--sh-3`(DEMO 패널) / `--sh-modal`(모달)
- 타이포: 시스템 폰트 스택(`Apple SD Gothic Neo` → `Malgun Gothic` → `Segoe UI`), `letter-spacing:-.01em`(한글 핀테크 톤), 볼드 700/800

### 디자인 언어 원칙 (실앱 캡처 근거 — `docs/references/image/`, 로컬 전용)
1. **흰 배경 + 큰 볼드 타이포 + 넉넉한 여백** (Toss/Kakao 계열 미감)
2. **둥근 pill 버튼** (`--r-pill`) — 진행/취소 동일 위계
3. **점선 구분**(`.sheet-row` dotted) — 실앱 주문 확인 시트 계승
4. **연회색 보조 텍스트**(`--sub`) — 라벨은 회색, 값은 볼드 잉크
5. **시그니처**: `.tag::before` 색점 = "사실/해석/모름"을 일관 신호화(게임화 아님·§14 준수). 출처 배지는 파랑 pill.

---

## 4. 이번 세션이 한 작업 (커밋 단위)

### S1 `aeb2b5c` — 디자인 파운데이션 전면 개편 (`app.css`)
- `:root` 토큰 시스템 재정의(위 §3).
- 공용 컴포넌트 전면 리스타일: 상단 3바·`#phone` 카드·진행 스텝퍼·`.kcard`·`.tag`(색점)·`.meta`(출처 배지)·`.price-head`(가격 28px 볼드)·`.btn`(pill)·`.intent-btn`·체크리스트·`.cmp` 비교표·입력·`.confirm-check`·중앙 모달·`.side-toggle`·`.sheet-row`(점선)·DEMO 패널.
- 접근성 바닥: `:focus-visible` 아웃라인·`prefers-reduced-motion`·`accent-color`.
- **기능성 규칙 전부 보존**: `[hidden]{display:none!important}`(T-0716-1510)·`body.safemode` 조상 숨김·중앙 모달(D-0718-0548/0210)·`body.expanded`·모든 id/클래스 훅.

### S2 `7329ac6` — S0·⑧ 주문 하단 CTA 좌우 병렬 (`index.html`+`app.css`)
- `index.html`: s0/s8 구매·판매 버튼을 `<div class="order-cta-row">`로 래핑(**id·disabled·JS 배선 불변**, 텍스트 추가 없음).
- `app.css`: `.order-cta-row { flex; flex-direction:row-reverse }` → **판매(파랑) 좌 / 구매(빨강) 우** = 실앱 캡처(27) 하단 CTA 동일 배치. 동일 크기·색 구분만(§14 side 강조 금지 준수).
- 테스트 안전성 사전 확인: `test_dom_constraints.py`는 버튼 `id`·`disabled`·JS 배선·`"order-tab" not in html`만 검사(부모 구조 무검사) → 래퍼 추가 안전. `.order-cta-row` ≠ `order-tab`.

### S3 `5d0ac41` — S0·⑧ 실앱 종목 상세 크롬 재현 (`app.js`+`app.css`)
- `app.js` `renderOrderReplica(prefix)`(S0·⑧ 공용 — 한 함수로 두 화면): 캡처27 구조의 재현 마크업으로 교체 — 네비 행(뒤로·검색·노랑 북마크·더보기 **인라인 SVG** — 오프라인 OK)·종목명+캐럿·**34px 대형 가격**·등락(기존 `pctChange()` 4중 표기 유지)+SOR 라벨·**정보/차트/호가/보유/토론 탭**(정보 활성 밑줄·빨간 점)·거래량/시총 행([데모 고정] 장식)·**SVG 차트**(`replicaChartSvg()` 신설 — change_pct 부호로 방향·색, 면 채움, 표시 전용)·기간 칩(1주 활성).
- **전부 비기능 장식**: `aria-hidden` + `span`(버튼 아님·배선 없음 — §14·재현 화면 규칙 준수). 동적 값은 전부 `esc()`/포맷터 경유(XSS 표면 없음 — 기존 패턴 동일).
- `app.css`: `.kp-nav/.kp-ic/.kp-caret/.kp-tabs(.kp-tab.on 밑줄)/.kp-dot/.kp-stat-row/.kp-chart/.kp-range(-chip.on)` + `.price-head.kp` 변형. 탭은 `#phone` 패딩까지 풀블리드(음수 마진 -18px — #phone 패딩 바뀌면 함께 수정).
- 클래스명 주의: `order-tab` 부분문자열 회피를 위해 `kp-` 접두(테스트 `"order-tab" not in html`).

---

## 5. 남은 작업 (§0 방향 상향 반영 — "전 화면 실앱급 재구축"이 본작업)

각 작업 후 **pytest 302 + 게이트 + 브라우저 하드리로드 확인 + 커밋·푸시**를 반복할 것.

### 5-0. ✅ [완료] 캡처 26장 정밀 분석 → 디자인 스펙 확보됨
**`docs/plans/redesign-design-spec.md`** (커밋 `7c239ed`) — 캡처 26장 6그룹 병렬 분석을 합성한 **픽셀 재현 기준서 v1.0**. 색 hex 확정값·타이포 11레벨·간격/라운드/그림자 원칙("그림자는 4곳만"·"버튼은 pill 아니면 소형 라운드 — 중간 없음")·구분선 4종·컴포넌트별 CSS 스펙(네비·종목 헤더·탭 2종·확인 시트·호가·리스트 행·달력 등) 포함. 개인 데이터 스캔 통과.
**이후 모든 화면 작업은 이 문서를 단일 기준으로 사용한다** (s4-ui-spec §3의 정밀화 부속 — 충돌 시 헌법 §14 > 계약 > s4-ui-spec > 본 스펙 순). 그룹별 상세 원본(합성 전 6개 분석)이 더 필요하면: `C:\Users\gorhk\.claude\projects\C--Users-gorhk-AX-Hackathon-Jocoding-AX----0-Main-Kakaopay-Securities\bcd72dcf-5367-4ff2-97a1-69edc704d86c\subagents\workflows\wf_285637b0-2be\journal.jsonl`의 result 레코드 6건.

### 5-1. 화면별 재구축 (우선순위순)
1. **[상] ⑥ 주문 확인 모달 → 실앱 시트(캡처 30·31) 완전 재현**: 상단 핸들바(회색 알약), "종목명 + N주 구매/판매"(색 텍스트) 헤더, **"총 주문 금액" 대형 행**, 점선 상세 행 리듬, "주문 자세히 보기" 아코디언 스타일(장식), 하단 닫기(회색)+구매/판매(색) **폭 비율 약 1:2.4** 버튼. **불변**: 비가역 확인 체크박스·고지 4종·중앙 모달(D-0718-0548)·`btn-settle`/`btn-sheet-back` id·`data-role="order-execute"`.
2. **[상] ② 브리핑 → 실앱 'AI분석' 카드(캡처 26) 톤 이식**: 볼드 헤드라인(등락 수치만 색), 본문 회색 2줄 리듬, 'AI분석' 스타일 배지를 출처·기준시각 배지에 적용.
3. **[상] ① 종목·계획 — S3에서 만든 `.kp-*` 크롬 재사용**: `renderStep1`의 `s1-header`도 `renderOrderReplica`류 크롬으로 통일(단 ①은 판단 레이어 진입 후이므로 네비/탭 밀도는 판단에 방해 안 되게 조절 판단).
4. **[중] ④ 비교표**: 실앱 리스트 행 밀도(캡처 35 손익 달력 톤). **열 대칭·강조색 없음 원칙 유지.**
5. **[중] ⑤·⑦**: 의향 버튼·일지 카드·회고 대조 카드 실앱 카드 리듬.
6. **[중] S0 인터셉트·⑥ 모달 진입 모션**: 실앱 바텀시트 감성의 절제된 fade/slide(`prefers-reduced-motion` 존중, 이미 CSS에 훅 있음).
7. **[하] 진행 스텝퍼·DEMO 패널·안전모드**: 실앱 칩/세그먼트 스타일로.
8. **[하] ⑧ 판단 기록 상기 카드**(`s8-judgment`) — 실앱 카드 톤(재수행 유도 배너 금지).

### 5-2. S3 잔여 디테일(작은 것)
- `.kp-stat-row`의 거래량/시총은 **[데모 고정] 장식 값** — 계약 §13 병기 관례에 따라 필요 시 재검토.
- 차트는 change_pct 부호로 방향·색 결정(표시 전용) — 시나리오 전환 시 자동 추종함. 데이터 기반 폴리라인(스냅샷 시계열)로 승격은 선택.
- 캡처 27 대비 미재현: 최고/최저 주석·우측 가격축·기간 칩별 등락률 병기(전부 숫자 발명 리스크로 보류 — 필요 시 fixture에 시계열 추가 후).

---

## 6. 절대 지켜야 할 제약 (온보딩 §4·§5·§6 요약 — 위반 시 무효/실격)

- **건드리지 말 것**: `src/engine/`·`src/policy/`·`src/briefing/`·`app.py` 로직·`docs/requirements-contract.md`·`tests/`·`tools/`·`logs/`·`.env`·운영 로그(append-only).
- **id 불변**: `index.html`의 기존 element `id` 제거·변경 금지(app.js·테스트가 참조). 래퍼·클래스 **추가**만 허용.
- **app.js**: 렌더 마크업 문자열의 클래스 추가만(디자인 필요 시). 상태·흐름·API·계약 표기·id 참조 불변.
- **오프라인 자기완결**: CDN·웹폰트·원격이미지·아이콘폰트 금지. 로컬·시스템폰트·인라인·data URI만. **네트워크 요청 0 유지.**
- **§14 제품 경계**: 방향 결론·권유·수익예측·**게임화(콘페티·축하·뱃지·뽑기) 금지**. 손실 화면 추가매수 기본강조 금지. 톤 차분·중립·신뢰.
- **접근성**: 손익 = 색+부호(±)+아이콘(▲▼)+텍스트 4중. 색만으로 정보전달 금지. 상승 적/하락 청 유지.
- **상시 표기 보존**: "모의 주문 — 실제 거래 아님" 바·"교육용 가상 데이터" 배지·안전 지표 카운터·출처/기준시각 배지·⑥ 고지 4종·⑥ 비가역 확인 체크박스·S0/⑧ 재현 라벨.
- **금지어**: `src/policy/lexicon.py` 사전에 걸리는 문구를 화면 텍스트로 추가 금지(게이트가 차단). 기존 문구는 스타일만.
- **실계좌 정보**: `docs/references/image/`는 **디자인(색·레이아웃)만 차용**, 실계좌번호·잔고·보유·개인정보 데모 복사 금지. 커밋·푸시 금지.

---

## 7. 롤백 (원본 데모가 항상 폴백)

- 디자인이 시연 리스크를 키우면: `git checkout main` (원본 동작 데모 그대로 복원).
- app.css만 원복: `cp out/app.css.orig.bak src/webapp/static/app.css` (백업은 gitignored).
- 특정 커밋만 되돌리기: `git revert 7329ac6`(주문 CTA만) 또는 `git revert aeb2b5c`(파운데이션).

---

## 8. 다음 세션 시작 체크리스트

1. `git checkout wt/redesign && git pull` (origin 최신 반영)
2. 서버 기동(포트 9200) + 브라우저 하드리로드로 현 상태 눈으로 확인
3. `pytest -q`(302) + `run_gate.py`(통과) 베이스라인 확인
4. §5 남은 작업에서 하나 골라 **화면 단위**로 착수 → 확인 → 커밋·푸시(작은 단위)
5. 각 단위마다 §6 제약·§2 검증 반복. 계약/계획과 어긋나면 문서 먼저 갱신(헌법 §2).
6. 로그 수집: worktree 삭제 전 `logs/` 반드시 복사(헌법 §13-4 불가침).
