# Local conversation logs
logs/

# Python cache
__pycache__/
*.py[cod]

# 가상환경
.venv/

# Local secrets
.env
.env.*
!.env.example
secrets/
*.key
*.pem

# 실계좌 정보가 담긴 앱 캡처 — 로컬 참고 전용, 커밋·푸시 금지 (헌법 §7)
docs/references/image/

# 실행 결과·임시 산출물
out/

# OS·에디터·브라우저 자동화 산출물
.DS_Store
Thumbs.db
.playwright-mcp/

# Claude Code 개인 로컬 설정(권한 허용 캐시) — 공유 설정 settings.json(로그 훅)은 추적 유지 (헌법 §10)
.claude/settings.local.json

# 실데이터 스냅샷(Yahoo 유래 시세) — PUBLIC 저장소 재배포 방지, 로컬 전용 (사용자 지시 2026-07-18)
# manifest(수집 시각·해시 증빙 — 데이터 아님)와 .gitkeep은 추적 유지
data/snapshots/*.json
!data/snapshots/manifest.json

# 실종목 시나리오 fixture(스냅샷 파생 — 실종가 250개 포함)도 동일 논리로 로컬 전용 (D-0718-1110)
# 재현 경로: scripts/data/collect_stock_snapshot.py → build_real_scenario.py (스크립트는 추적 유지)
data/fixtures/scenario_real_*.json

# 실LLM 동반자 문답 캐시(실데이터 수치 포함 가능) — 생성 스크립트는 추적 유지
data/fixtures/companion_cache/
